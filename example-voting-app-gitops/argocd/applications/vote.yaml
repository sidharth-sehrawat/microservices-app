apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: vote
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/sidharth-sehrawat/projects.git
    targetRevision: master
    path: example-voting-app-gitops/vote

  destination:
    server: https://kubernetes.default.svc
    namespace: vote

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
